import React from "react";
import styled from "styled-components";
import { Button, Modal, Paper } from "@material-ui/core";

export const GridPaper = styled(props => <Paper {...props} />)`
  padding: 10px 20px 15px 20px;
`;
